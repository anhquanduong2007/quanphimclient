const styles = () => {
  return {
    movie__body: {
        justifyContent: "center",
    },
    movie__content: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
    },
    list__movie: {
      justifyContent: "space-around",
    }
  };
};
export default styles;
